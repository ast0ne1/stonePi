[project]
name = "stonepi-auth"
version = "0.1.0"
description = "Shared StonePi session cookie, login redirects, and URL prefix rewriting"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["stonepi_auth*"]
